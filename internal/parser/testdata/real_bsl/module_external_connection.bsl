Функция ПолучитьДанныеВнешнегоПодключения(Идентификатор, Пароль) Экспорт
	WSСоединение = Новый WSСоединения("http://example.com/ws", Идентификатор, Пароль);
	Прокси = Новый HTTPСоединение("proxy.local", 3120);
	Результат = WSСоединение.Вызвать("GetData");
	Возврат Результат;
КонецФункции
Процедура ЗагрузитьФайлПоФТП(АдресСервера, Путь)
	ФТПСоединение = Новый FTPСоединение(АдресСервера);
	ФТПСоединение.Скачать(Путь, ПолучитьИмяВременногоФайла());
	ФТПСоединение.Закрыть();
КонецПроцедуры
