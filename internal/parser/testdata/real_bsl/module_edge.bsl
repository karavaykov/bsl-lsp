// Модуль с различными типами данных, выражениями и граничными случаями
Процедура ТестТиповДанных()

	Строка1 = "Обычная строка";
	Строка2 = "Строка с ""кавычками"" внутри";
	Строка3 = "Строка
	|с переносом";

	Число1 = 42;
	Число2 = 3.14;
	Число3 = -7;

	Дата1 = '20241001';
	Дата2 = '20241001120000';
	Дата3 = '00010101';

	Булево1 = Истина;
	Булево2 = Ложь;
	НеОпределено1 = Неопределено;
	Нул1 = Null;

КонецПроцедуры

Процедура ТестВыражений()

	// Арифметика
	А = 10 + 20 * 3 - 5 / 2;
	Б = (10 + 20) * (3 - 5) / 2;
	В = -А + Б;

	// Сравнение
	Равно = 5 = 5;
	НеРавно = 5 <> 3;
	Меньше = 3 < 5;
	Больше = 5 > 3;
	МеньшеИлиРавно = 3 <= 5;
	БольшеИлиРавно = 5 >= 3;

	// Логические
	И = Истина И Ложь;
	Или = Истина ИЛИ Ложь;
	Не = Не Истина;

	// Конкатенация
	Имя = "Иван" + " " + "Петров";

	// Тернарный
	Макс = ?(А > Б, А, Б);

КонецПроцедуры

Процедура ТестКонструкций()

	// Goto с меткой
	Если 1 > 0 Тогда
		Перейти ~Метка1;
	КонецЕсли;
	~Метка1:
	Возврат;

	// Raise
	ВызватьИсключение "Тестовое исключение";
	ВызватьИсключение;

	// Выполнить
	Выполнить("а = 1");

	// Адрес, Тип, ЗначениеВСтрокуВнутр
	Адрес1 = АдресВСтрокеВнутр(СсылкаНаОбъект);
	Тип1 = Тип("Строка");
	Значение1 = ЗначениеВСтрокуВнутр(СсылкаНаОбъект);

КонецПроцедуры

Процедура ТестСложныеИмена()

	А_1 = 1;
	АБВГД = 2;
	_Частное = 3;
	ОченьДлинноеИмяПеременной123 = 4;

	// Имена методов в стиле camelCase невалидны в 1С, но пусть парсер их пропустит как идент
	// myVariable = 5; // Это не BSL, но если попадет — не упадет

КонецПроцедуры

Процедура ТестКаждыйЦикл(Таблица)
