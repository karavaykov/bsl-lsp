Процедура ПоказатьСообщениеПользователю(Текст, Статус)
	Если Статус = "Ошибка" Тогда
		Предупреждение(Текст);
	ИначеЕсли Статус = "Вопрос" Тогда
		Ответ = Вопрос(Текст, РежимДиалогаВопрос.ДаНетОтмена);
		Если Ответ = КодВозвратаДиалога.Да Тогда
			ВыполнитьДействие();
		КонецЕсли;
	Иначе
		Сообщить(Текст);
	КонецЕсли;
КонецПроцедуры
Процедура ОткрытьФормуВыбора(ИмяФормы, Параметры)
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("РежимОткрытия", РежимОткрытияФормы.Выбор);
	ПараметрыФормы.Вставить("ПараметрыВыбора", Параметры);
	ОткрытьФорму(ИмяФормы, ПараметрыФормы);
КонецПроцедуры
